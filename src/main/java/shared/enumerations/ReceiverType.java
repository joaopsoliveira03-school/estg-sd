package shared.enumerations;

/**
 * Enum representing the type of receiver for a message.
 * The possible values are USER, GROUP, and BROADCAST.
 */
public enum ReceiverType {
  USER, GROUP, BROADCAST
}
